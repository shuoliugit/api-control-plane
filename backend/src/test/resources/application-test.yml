spring:
  datasource:
    url: jdbc:h2:mem:controlplane;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379

app:
  admin:
    email: admin@controlplane.local
    password: AdminPassword123!
  jwt:
    issuer: api-control-plane-test
    secret: test-secret-test-secret-test-secret-test-secret-test-secret
    ttl-minutes: 30
  sandbox:
    rate-limit-per-minute: 3
